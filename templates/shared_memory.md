# Shared Long-Term Memory

<!-- 
  This is the main shared memory file. All AI entry points read from and write to this file.
  
  RULES:
  - Append only. Never delete, overwrite, or reorder existing entries.
  - Format: [YYYY-MM-DD HH:MM] one-line summary
  - Archive older entries to shared_memory/YYYY-MM.md when this file exceeds ~100 entries.
-->

<!-- [YYYY-MM-DD HH:MM] Initialized shared memory system. -->
